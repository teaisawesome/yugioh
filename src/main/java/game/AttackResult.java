package game;

/**
 * Class representing the battle results if two monster fight with each other.
 * WIN - if one of monster win the battle.
 * DRAW - if the battle is draw.
 * FAIL - if one of monster fail the attack and lose the battle.
 */
public enum AttackResult
{
    WIN,
    DRAW,
    FAIL
}
